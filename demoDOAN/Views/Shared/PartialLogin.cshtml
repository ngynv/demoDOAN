@using demoDOAN.Models
@{
    <link href="~/Content/bootstrap.css" rel="stylesheet" />
    <link href="~/Content/StyleSheet.css" rel="stylesheet" />
    var user = Session["Account"] as demoDOAN.Models.Customer;
}

<div class="float-end" style="">
    @if (user != null)
    {
        //Customer cust = Session["TaiKhoan"] as Customer;
        <a href="@Url.Action("Details","Customers",new { id = user.IDCus})" class="text-none" style="text-decoration: none; /* font-size: 12px; */ color: #6f6f6f; font-weight: 700; padding-left: 5px; font-size: 17px">@user.NameCus</a>
        @*<a href="@Url.Action("Logout","Customers")" class="text-none" style=" padding: 5px 15px; background-color: #ffc107; color: #fff; text-decoration: none; margin-left: 35px; border-radius: 5px; font-weight: 700; font-size: 17px ">Thoát</a>*@
    }

</div>
